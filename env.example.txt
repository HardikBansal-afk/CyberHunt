# Copy to .env in the repo root and fill in real values. Never commit .env.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/cyberhunt

# Signs every JWT. MUST be long and random in production - anyone with this
# value can forge a token for any user. Generate one with:
#   python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET_KEY=change-me-to-a-long-random-string

# How long a login stays valid, in hours.
JWT_EXPIRES_HOURS=12

# Frontend origin allowed to call the API, e.g. http://localhost:5173
CORS_ORIGINS=*
